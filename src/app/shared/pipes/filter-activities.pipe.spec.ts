import { FilterActivitiesPipe } from './filter-activities.pipe';

describe('FilterActivitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterActivitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
