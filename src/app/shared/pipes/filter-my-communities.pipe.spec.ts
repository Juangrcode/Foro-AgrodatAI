import { FilterMyCommunitiesPipe } from './filter-my-communities.pipe';

describe('FilterMyCommunitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMyCommunitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
